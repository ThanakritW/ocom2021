#include<bits/stdc++.h>
using namespace std;
int main(){
    cin >> n >> m;
    for(int i=0;i<m;i++){
        cin >> ta >> tb >> op;
        if(op=='='){
            pa=fp(ta);
            pb=fp(tb);
            if(ta!=tb){
                p[pa]=pb;
            }
        }else if(){

        }else{
            
        }
    }
    return 0;
}